#include <stdlib.h> 
#include <vector>

class Helper {
    public:
     static int getNum(char[]);
     static bool isValidDig(char);
     static bool isNeg(std::vector<char>& , int);
     static bool isMult(std::vector<char>& , int);
};